public interface CompareElements {

	   // @returns
	   //   < 0 if first comes before second
	   //   0 no preference in relative position
	   //   > 1 if second comes before first
	   int compare(AccFootballTeam first, AccFootballTeam second);
	
}